# ============================================================================
# Achievement rewards
# ----------------------------------------------------------------------------
# Reload after editing with /nradmin reload.
#
# Commands are dispatched from the console.
# Placeholders available in commands:
#   %player% - the player's name
#   %uuid%   - the player's UUID
#
# Broadcasts are deserialized as MiniMessage and sent to every online player.
# Placeholders available in broadcasts:
#   %player%      - the player's name
#   %uuid%        - the player's UUID
#   %achievement% - the hoverable, clickable achievement component
#
# Achievement ids can be viewed in-game via /nr achievements.
# ============================================================================

# Default commands run when a gained achievement mastery tier does not complete a specific reward.
default-commands: []
#  - "broadcast %player% earned an achievement!"
#  - "give %player% diamond 1"

# Default broadcasts sent when no specific reward is completed.
default-broadcasts: []
#  - "<yellow>%player%</yellow> earned %achievement%!"

# Default display names are MiniMessage descriptions shown on every achievement.
default-display-names: []
#  - "<yellow>1 Crown</yellow>"

# Each reward runs its commands when a player gains an achievement tier that EXACTLY matches
# one of the reward's 'requirements' (achievement id + class + tier), AND every requirement is
# met at or above its tier. Matching the exact tier means each reward fires once.
#
# Requirement keys mirror unlocks: "achievement_id: <tier>" for global (or auto-resolved class)
# scope, or "achievement_id@CLASS: <tier>" to require a specific class's version.
# Display names are user-facing MiniMessage descriptions of the command rewards. The list may be empty.
rewards: {}
#  completionist:
#    requirements:
#      victorious: 4
#      conqueror: 1
#    display-names:
#      - "1 Nether Star"
#    commands:
#      - "give %player% nether_star 1"
#    broadcasts:
#      - "<yellow>%player%</yellow> completed %achievement%!"
#  thief_low_district:
#    requirements:
#      low_district_victor@THIEF: 1
#    display-names: []
#    commands:
#      - "give %player% diamond 1"